[project]
name = "dantri-vote-batch"
version = "0.1.0"
description = "CLI tool for staged account registration and vote-flow testing"
requires-python = ">=3.11"

[tool.pytest.ini_options]
pythonpath = ["."]
